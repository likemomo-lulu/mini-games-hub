/* 打地鼠游戏样式 */

/* 容器 */
.container {
  height: 100vh;
  overflow: hidden;
  padding: 30rpx 20rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 头部信息栏 */
.header {
  width: 100%;
  margin-top: 20rpx;
  margin-bottom: 40rpx;
}

.stats {
  display: flex;
  justify-content: flex-end;
  gap: 15rpx;
}

.stat-box {
  text-align: center;
  padding: 15rpx 25rpx;
}

.stat-label {
  display: block;
  font-size: 20rpx;
}

.stat-value {
  display: block;
  font-size: 32rpx;
  font-weight: bold;
}

/* 控制按钮 */
.controls {
  width: 100%;
  display: flex;
  margin-top: 40rpx;
  margin-bottom: 30rpx;
}

.btn {
  flex: 1;
  padding: 0;
  border-radius: 25rpx;
  font-size: 28rpx;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  display: flex;
  justify-content: center;
  align-items: center;
}

.btn:active {
  opacity: 0.8;
  transform: scale(0.98);
}

.btn[disabled] {
  opacity: 0.5;
}

/* 游戏区域 */
.game-board {
  position: relative;
  border-radius: 30rpx;
  padding: 30rpx;
  width: 100%;

}

/* 3x3 地洞网格 */
.hole-grid {
  width: 600rpx;
  height: 600rpx;
  position: relative;
  border-radius: 30rpx;
  padding: 30rpx;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: repeat(3, 1fr);
  gap: 20rpx;
  background: var(--panel-bg, rgba(255, 255, 255, 0.25));
  aspect-ratio: 1;
}

/* 地洞 - 使用图片 */
.hole {
  position: relative;
  width: 100%;
  padding-top: 100%;
  overflow: hidden;
}

.hole-image {
  position: absolute;
  bottom: -20rpx;
  left: 0;
  width: 100%;
  height: 100%;
}

/* 地鼠 */
.mole {
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
  width: 200rpx;
  height: 200rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  transition: top 0.2s ease;
}

.mole.up {
  top: 10%;
}

.mole.down {
  top: 100%;
}

.mole-icon {
  font-size: 100rpx;
  filter: drop-shadow(0 4rpx 8rpx rgba(0, 0, 0, 0.3));
}

/* 金色地鼠 */
.hole.luck .mole-icon {
  filter: drop-shadow(0 0 20rpx var(--gold-from, #FFD700)) drop-shadow(0 4rpx 8rpx rgba(0, 0, 0, 0.3));
}

/* 炸弹 */
.hole.pig .mole-icon {
  animation: shake 0.5s ease infinite;
}

@keyframes shake {
  0%, 100% { transform: rotate(0deg); }
  25% { transform: rotate(-10deg); }
  75% { transform: rotate(10deg); }
}

/* 打击特效 */
.hit-effect {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0);
  display: flex;
  justify-content: center;
  align-items: center;
  pointer-events: none;
  opacity: 0;
  transition: all 0.3s ease;
}

.hit-effect.show {
  transform: translate(-50%, -50%) scale(1);
  opacity: 1;
}

.hit-text {
  font-size: 40rpx;
  font-weight: bold;
  color: var(--gold-from, #FFD700);
  text-shadow: 0 0 20rpx rgba(255, 215, 0, 0.8);
}

/* 覆盖层 */
.overlay {
  border-radius: 30rpx;
}

.overlay-content {
  text-align: center;
}

.overlay-title {
  font-size: 60rpx;
  margin-bottom: 20rpx;
}

.overlay-text {
  font-size: 32rpx;
  margin-bottom: 30rpx;
}

.overlay-info {
  font-size: 28rpx;
  margin-bottom: 15rpx;
}

/* 操作提示 */
.tips {
  text-align: center;
  margin-top: 30rpx;
  font-size: 24rpx;
}
